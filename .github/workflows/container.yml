name: container
on:
  push:
    branches: [main]

jobs:
   job1:
      runs-on: ubuntu-latest
      container:
        image: hub.docker.com/r/antviosona/jdk11node10
        credentials:
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}
          
      steps:
        - name: printing
          run: echo "hi"

      

  
          
